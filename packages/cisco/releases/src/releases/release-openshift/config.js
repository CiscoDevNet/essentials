/**
 * @file Configuration options specific to OpenShift.
 * @author Matt Norris <matnorri@cisco.com>
 */

const {
  BOT_URL,
  COMMAND_EVENTS,
  EXEC_SYNC_OPTIONS,
  PORT,
  DEPLOYMENT,
  DEPLOYMENT_TEMPLATE,
  ROUTE,
  ROUTE_TEMPLATE,
  SECRET,
  SERVICE,
} = require("../config");

module.exports = {
  BOT_URL,
  COMMAND_EVENTS,
  EXEC_SYNC_OPTIONS,
  PORT,
  DEPLOYMENT,
  DEPLOYMENT_TEMPLATE,
  ROUTE,
  ROUTE_TEMPLATE,
  SECRET,
  SERVICE,
};
