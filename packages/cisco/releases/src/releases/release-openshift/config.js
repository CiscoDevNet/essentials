/**
 * @file Configuration options specific to OpenShift.
 * @author Matt Norris <matnorri@cisco.com>
 */

const {
  COMMAND_EVENTS,
  EXEC_SYNC_OPTIONS,
  DEPLOYMENT,
  DEPLOYMENT_TEMPLATE,
  ROUTE,
  ROUTE_TEMPLATE,
  SECRET,
  SERVICE,
} = require("../config");

module.exports = {
  COMMAND_EVENTS,
  EXEC_SYNC_OPTIONS,
  DEPLOYMENT,
  DEPLOYMENT_TEMPLATE,
  ROUTE,
  ROUTE_TEMPLATE,
  SECRET,
  SERVICE,
};
