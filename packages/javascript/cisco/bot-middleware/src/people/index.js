const Standard = require("./standard-people");
const Webex = require("./webex-people");

const { hasAllProps, joinArrays } = require("./shared");

module.exports = {
  Standard,
  Webex,
  hasAllProps,
  joinArrays,
};
