/**
 * @file Constants used by the basic Command
 * @author Matt Norris <matnorri@cisco.com>
 */

const {
  ATTACHMENT_EVENT,
  INTENT_DETECTED_EVENT,
  INTENT_MATCHING_TYPES,
  STANDARD_MESSAGE_TYPES,
} = require("../constants");

module.exports = {
  ATTACHMENT_EVENT,
  INTENT_DETECTED_EVENT,
  INTENT_MATCHING_TYPES,
  STANDARD_MESSAGE_TYPES,
};
