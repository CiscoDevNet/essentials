const Release = require("./releases/release");
const GoogleRelease = require("./releases/release-google");
const OpenShiftRelease = require("./releases/release-openshift");

module.exports = {
  Release,
  GoogleRelease,
  OpenShiftRelease,
};
